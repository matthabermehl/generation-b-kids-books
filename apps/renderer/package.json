{
  "name": "@book/renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "format": "echo 'format: @book/renderer no-op'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.750.0",
    "pdfkit": "^0.15.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pdfkit": "^0.17.3",
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  }
}
