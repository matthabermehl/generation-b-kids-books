{
  "name": "@book/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "format": "echo 'format: @book/domain no-op'"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  }
}
